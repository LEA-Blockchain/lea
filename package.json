{
  "name": "@getlea/lea",
  "version": "1.0.2",
  "description": "Command-line tool for the Lea blockchain",
  "type": "module",
  "bin": {
    "lea": "./dist/cli.mjs"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "clean": "del-cli ./dist",
    "build": "node scripts/build.mjs",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=19.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "@getlea/keygen": "^1.0.0",
    "@getlea/web3": "^1.0.5"
  },
  "devDependencies": {
    "del-cli": "^6.0.0",
    "esbuild": "^0.25.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
